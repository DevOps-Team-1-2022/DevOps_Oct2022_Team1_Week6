name: Deployment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  Approval:
    name: Seek Approval for Release
    if: github.event.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: dev
    
  CreateDevRelease:
    name: Create Dev Release
    needs: [Approval]
    runs-on: ubuntu-latest
    steps:
      - name: Download Report
        run: echo Downloading... 
      - name: Create Zip
        run: echo Zipping...
      - name: Create Dev Release
        run: echo Releasing... 
